// StreamingQueryForm is superseded by the inline implementation in app/stream/page.tsx.
// This stub is kept so any stale imports don't break the build.
export default function StreamingQueryForm() {
  return null
}
